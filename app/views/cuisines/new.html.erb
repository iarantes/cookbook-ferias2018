<h1>Nova Cozinha</h1>
<p><%= alert %></p>

<% @cuisine.errors.full_messages.each do |erro| %>
  <p><%= erro %></p>
<% end %>

<%= form_for @cuisine do |f| %>
  <div>
    <%= f.label :name, "Nome" %>
    <%= f.text_field :name %>
  </div>
  <%= f.submit "Enviar" %>
<% end %>