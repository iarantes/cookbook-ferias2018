<h1>Novo tipo de receita</h1>
<p><%= alert %></p>

<% @recipe_type.errors.full_messages.each do |erro| %>
  <p><%= erro %></p>
<% end %>

<%= form_for @recipe_type do |f| %>
  <div>
    <%= f.label :name, 'Nome' %>
    <%= f.text_field :name %>
  </div>
  <%= f.submit 'Enviar'%>
<% end %>